# Authoring Prompts

This directory lives **outside** Hugo's `content/` tree on purpose — these files are authoring artifacts, not published pages. Each prompt is the seed input that produced an issue, kept in-repo so the recipe is reproducible.

When working on a new issue, drop the prompt here as `promptNN_topic.md`. The website build never touches this directory.
