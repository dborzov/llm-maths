[project]
name = "almanac-scripts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]

[project.optional-dependencies]
torch = [
    "torch>=2.2",
]

[dependency-groups]
dev = []
